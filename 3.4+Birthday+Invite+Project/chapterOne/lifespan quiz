
function lifeInWeeks(age) {    
   
    var lifespan = 90;
    var totalTimeLeft;
    var ageInDays = age *365;
    var timeLeftWeeks = (lifespan*52) - (age*52);
    var timeLeftMonth = lifespan*12 - (age*12);
    var timeLeftDays = (lifespan*365) - (ageInDays);
    
    console.log("You have " + timeLeftDays + " days, " + timeLeftWeeks + " weeks, and " +timeLeftMonth+ " months left.");
     

}
lifeInWeeks(45);

var num = Math.random();
num = num*6;
